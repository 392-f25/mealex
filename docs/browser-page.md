# Browser Page for MealEx

- Display a grid of profile cards with no functionality.
  - Each profile card should have the user's name, major, year, and some optional tags.
  - Profile cards should be a reusable react component.
- Display a "manage profile" button at the top right corner with no functionality.